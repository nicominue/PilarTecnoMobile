import React, { Component, useCallback } from 'react';
import { 
    View, 
    Text
} from 'react-native';
import Home from '../screens/Home'

export default App = () => {

    return(
        <Home />
    )
}